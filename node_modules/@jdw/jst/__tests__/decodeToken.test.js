"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const index_1 = require("./../index");
describe('@jdw/jst/decodeToken', () => {
    it('can decode an encoded / character', () => {
        chai_1.expect(index_1.decodeToken('foo~1bar')).to.eq('foo/bar');
        chai_1.expect(index_1.decodeToken('~1foo~1bar')).to.eq('/foo/bar');
    });
    it('can decode an encoded ~ character', () => {
        chai_1.expect(index_1.decodeToken('~0home')).to.eq('~home');
        chai_1.expect(index_1.decodeToken('~0hello~0goodbye')).to.eq('~hello~goodbye');
    });
    it('can decode a mixed ~ and / character string', () => {
        chai_1.expect(index_1.decodeToken('~0~1home')).to.eq('~/home');
    });
});
//# sourceMappingURL=decodeToken.test.js.map