"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const index_1 = require("./../index");
describe('@jdw/jst/encodeToken', () => {
    it('can encode the / character', () => {
        chai_1.expect(index_1.encodeToken('foo/bar')).to.eq('foo~1bar');
        chai_1.expect(index_1.encodeToken('/foo/bar')).to.eq('~1foo~1bar');
    });
    it('can encode the ~ character', () => {
        chai_1.expect(index_1.encodeToken('~home')).to.eq('~0home');
        chai_1.expect(index_1.encodeToken('~hello~goodbye')).to.eq('~0hello~0goodbye');
    });
    it('can encode both the ~  and / characters in the one string', () => {
        chai_1.expect(index_1.encodeToken('~/home')).to.eq('~0~1home');
    });
});
//# sourceMappingURL=encodeToken.test.js.map