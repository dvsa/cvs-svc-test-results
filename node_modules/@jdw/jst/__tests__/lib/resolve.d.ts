/**
 * A resolve function must simply take a schema id as an argument and return
 * that schema as an object literal or throw an error if it can't find it.
 */
declare const resolve: (id: any) => object;
export { resolve };
