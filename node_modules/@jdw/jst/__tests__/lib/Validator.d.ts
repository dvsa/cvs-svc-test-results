/**
 * A validation helper for our test schema.
 */
export declare class Validator {
    schema: object[];
    private ajv;
    constructor(config?: object);
    load(schema: object[]): this;
    getSchema(id: string): object | undefined;
}
