"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const index_1 = require("./../index");
describe('@jdw/jst/set', () => {
    const fixture = {
        array: [1, 2, null],
        object: {
            foo: null,
            array: [{ foo: null }, null],
        },
        foo: null,
    };
    it('can set an property on an object', () => {
        index_1.set(fixture, '#/foo', 'bar');
        chai_1.expect(fixture.foo).to.eq('bar');
    });
    it('can set an index on an array', () => {
        index_1.set(fixture, '#/array/2', 3);
        chai_1.expect(fixture.array[2]).to.eq(3);
    });
    it('can set an property on a nested object', () => {
        index_1.set(fixture, '#/object/foo', 'bar');
        chai_1.expect(fixture.object.foo).to.eq('bar');
    });
    it('can set an property on a nested array', () => {
        index_1.set(fixture, '#/object/array/1', 'bar');
        chai_1.expect(fixture.object.array[1]).to.eq('bar');
    });
    it('can set an property on an object in an array', () => {
        index_1.set(fixture, '#/object/array/0/foo', 'bar');
        chai_1.expect(fixture.object.array[0].foo).to.eq('bar');
    });
});
//# sourceMappingURL=set.test.js.map