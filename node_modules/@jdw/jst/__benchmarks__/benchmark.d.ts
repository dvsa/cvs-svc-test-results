/// <reference types="benchmark" />
import { Suite } from 'benchmark';
export interface ITest {
    id: string;
    deferred: boolean;
    exec: any;
}
declare const benchmark: (title: string, description: string, tests: ITest[]) => Suite;
export { benchmark };
